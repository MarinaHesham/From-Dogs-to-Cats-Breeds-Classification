#!/usr/bin/python3

# Create a model to classify dogs breeds
# Retrain the classification head and fine tune the model to classify cats breeds

def run_model(train_images, train_labels, test_images, test_labels):
    return